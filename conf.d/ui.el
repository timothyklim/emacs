(if window-system
  (tool-bar-mode -1)
  (menu-bar-mode -1))

(setq inhibit-splash-screen -1)

(scroll-bar-mode -1)

(global-linum-mode -1)

(setq ring-bell-function 'ignore)

(setq-default indent-tabs-mode nil)
